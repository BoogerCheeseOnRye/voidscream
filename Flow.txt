Every single run (copy-paste this block verbatim as your starting instruction):
Run flow means do these steps exactly every single time in this order and log them all before doing anything else:

Immediately fetch the exact current raw index.html from the github repo https://raw.githubusercontent.com/BoogerCheeseOnRye/voidscream/main/index.html (no cache, fresh).
Count every line and record the exact number.
Fetch and read the entire VoidChamber repo to pull neon glow card styles, onboarding particles and any matching code chunks as reference only.
Scan the full index.html line by line for bugs.
Note any differences between current repo file and the exact features built function by function yesterday.
Line count ignored per protocol update — always proceed.
Only when line count hits 1000 or more (or after any previous run) apply exactly these two changes and nothing else:
The first change is whatever the user explicitly asked for in this message (e.g. alter name validation, modal clearing, ensureCurrentAlter on every modal/button open past onboarding, feedback modal, etc.).
The second change is always the refined permanent Flow protocol text itself if the user requests a new version or refinement — otherwise keep the existing refined protocol block untouched.

Keep every other line pixel identical to the fetched file including all modals, feed, header, particles, localStorage functions, onclicks, clearModalTextFields on every modal open, ensureCurrentAlter() guard on every modal/button open past onboarding, alter-specific everything, and the permanent refined Flow block at the end.
Output the complete uncut full file as one single html block ready to copy-paste with no comments, no placeholders, no summaries.
End with the exact one-line confirmation: "The app is still exactly the same except those two fixes and flow is ready for next command now."

Additional permanent rules baked into every run (from all chat instructions):

On app open (window.onload): check if onboarding is done and there is at least 1 alter. If yes to both, do not load onboarding warnings/gates — go straight to app.
Every modal/button open past onboarding must call ensureCurrentAlter() and clearModalTextFields().
Alter names: minimum 1 character, allow any numbers/symbols that won’t break code (security-first sanitization kept).
All text fields empty on modal open.
All modals, buttons, and features 100% alter-specific.
Security first: no code-breaking symbols, localStorage only, offline only.

This refined protocol is now the single source of truth for Grok as the perfect engineer. Paste it at the top of every new conversation for instant perfect single-file app development. Flow ready for next command.
